This files contain two implementations of List interface,
one that uses arrays, and one that uses doubly-linked lists.
After that, ListTester tests the performance of several operations
by using different methods.